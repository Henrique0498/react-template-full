import { customRenderJest } from "@utils/JestUtils";

import {{pascalCase name}} from '.'

describe('<{{pascalCase name}} />', () => {
  it('should render', () => {
    const { container } = customRenderJest(<{{pascalCase name}} />)

    expect(container.firstChild)
  })
})